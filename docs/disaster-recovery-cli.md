# Disaster Recovery CLI

Commands include `backup-profiles`, `state-inventory`, `backup-preflight`, `backup-create`, `backup-verify`, `restore-preview`, `restore-drill`, `restore-execute`, `state-integrity-check`, `permission-restore-validate`, `evidence-continuity-check`, and `dr-evidence-bundle`.

All commands are local-only and support JSON output.

