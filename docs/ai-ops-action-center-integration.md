# AI/Ops Action Center Integration

AI/Ops command proposals can be converted to `ActionProposal` with source `ai_ops`.

AI/Ops can propose diagnostics and support actions, but cannot approve or execute them. Unsafe AI output is blocked by the same policy validator used for operator proposals.

