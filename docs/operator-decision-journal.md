# Operator Decision Journal

`DecisionJournal` appends every approval, rejection, defer, and evidence request to `decision-journal.jsonl`.

Entries include operator id, proposal id, decision, reason codes, previous and next status, redacted confirm acknowledgement, and a short hash.

Exports are available as JSON and Markdown.

