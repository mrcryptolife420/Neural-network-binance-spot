# Local Observability Dashboard

The dashboard observability panel shows local metrics status, metric aggregates, rows, anomalies, and evidence. It displays `LOCAL OBSERVABILITY ONLY` and keeps raw JSON inside an evidence expander.
