# Basket Builder

The basket builder supports deterministic modes: `top_score`, `diversified`, `conservative`, `high_volume`, `low_drawdown`, `model_balanced`, `strategy_balanced` and `custom`.

Filters include basket size, minimum paper score, data quality, drawdown cap, quote asset and symbol allow/deny lists.

