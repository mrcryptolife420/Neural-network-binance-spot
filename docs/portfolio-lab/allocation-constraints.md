# Allocation Constraints

Allocation constraints validate paper research weights before simulation.

Rules include total weight, per-symbol cap, minimum data quality, maximum symbol count, maximum strategy count, maximum model alias count and blocked candidate exclusion.

