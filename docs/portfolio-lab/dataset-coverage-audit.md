# Dataset Coverage Audit

The coverage audit reports candle counts, first and last timestamps, missing intervals, duplicates, staleness, gap severity and public-only proof for every symbol in the split.

