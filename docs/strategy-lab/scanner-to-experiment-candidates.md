# Scanner To Experiment Candidates

Scanner metrics from Roadmap 112 become `ScannerExperimentCandidate` records.

Candidates include symbol, source run, source preset, ranking reasons, metrics snapshot, data quality status, intervals, warnings, no-advice wording, and `live_trading_enabled=false`.
