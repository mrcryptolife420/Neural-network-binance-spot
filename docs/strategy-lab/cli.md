# Strategy Lab CLI

Commands:

```powershell
python -m binance_spot_bot.cli strategy-lab-candidates-build --json
python -m binance_spot_bot.cli strategy-lab-queue-preview --preset small_safe_smoke --json
python -m binance_spot_bot.cli strategy-lab-queue-create --preset small_safe_smoke --json
python -m binance_spot_bot.cli strategy-lab-queue-run --queue latest --confirm RUN_PAPER_EXPERIMENTS_ONLY --json
python -m binance_spot_bot.cli strategy-lab-results --json
python -m binance_spot_bot.cli strategy-lab-compare --json
python -m binance_spot_bot.cli strategy-lab-scorecards --json
python -m binance_spot_bot.cli strategy-lab-portfolio-candidates --json
python -m binance_spot_bot.cli strategy-lab-guards --json
python -m binance_spot_bot.cli dashboard-v2-strategy-lab-smoke --json
```
