# Strategy Lab Evidence Bundle

Evidence bundles include the safety contract, candidates, queue manifest, runner report, result summaries, comparison, scorecards, portfolio research, guards, and no-live proof.

CLI:

```powershell
python -m binance_spot_bot.cli strategy-lab-evidence-export --json
```
