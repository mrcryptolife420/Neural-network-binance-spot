# Experiment Matrix

Matrix presets:

- `small_safe_smoke`
- `scanner_top10_paper`
- `model_compare`
- `risk_compare`

Matrices expand scanner candidates into deterministic paper-only queue jobs while respecting max job budgets.
