# Paper Experiment Runner

The paper runner executes fixture-based local simulations.

It does not require API keys and does not call signed, account, or order endpoints.

Queue execution requires:

```text
RUN_PAPER_EXPERIMENTS_ONLY
```
