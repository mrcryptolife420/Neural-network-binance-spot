# Release Manifest

`create_release_manifest` writes a local release manifest with release id, version, compatibility, migration requirements, validation commands, artifacts, hashes, and no-live statement.

