# Ensemble Definition Schema

Roadmap: 099

`ensemble_config.py` defines:
- `EnsembleMember`
- `EnsembleVotingPolicy`
- `EnsembleWeightPolicy`
- `EnsembleConfig`

Validation blocks live aliases, excessive weights, low-health members and critical drift.
