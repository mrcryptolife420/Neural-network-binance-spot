# Dashboard V2 Browser UI

Open the local backend at `http://127.0.0.1:8800/`. The UI is served from `src/binance_spot_bot/dashboard_v2/static` and uses same-origin API calls with localhost-only fallback support.

