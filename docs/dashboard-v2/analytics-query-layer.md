# Analytics Query Layer

`dashboard_v2.analytics_query` reads local runtime snapshots and fixture data for candles, equity, signals, fills, risk blocks, alerts, sessions, model status, portfolio status, operator evidence, support status and performance metrics.

The query layer enforces tail limits, payload limits, redaction and no-live mode.
