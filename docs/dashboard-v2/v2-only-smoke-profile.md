# V2 Only Smoke Profile

Run `python -m binance_spot_bot.cli dashboard-v2-only-smoke --json`.

The profile verifies V2 import, operator mode, API routes, WebSocket heartbeat, no-live proof and no Streamlit import.
