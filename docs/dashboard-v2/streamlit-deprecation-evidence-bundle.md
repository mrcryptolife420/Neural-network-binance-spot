# Streamlit Deprecation Evidence Bundle

Run `python -m binance_spot_bot.cli dashboard-v2-deprecation-evidence-export --json`.

The bundle includes parity, Streamlit inventory, critical workflows, CLI router, operator mode, compat map, freeze policy, gate, V2-only smoke, fallback drill and no-live proof.
