# Support Bundle Integration

Run `python -m binance_spot_bot.cli dashboard-v2-support-diagnostics --json`.

Diagnostics include static build verification, performance budget status, browser smoke matrix, recent error reports and no-live proof. Missing optional artifacts are warnings.
