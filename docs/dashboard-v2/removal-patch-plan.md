# Removal Patch Plan

Run `python -m binance_spot_bot.cli dashboard-v2-removal-patch-plan --json`.

The plan lists files, validation commands and rollback archive requirements before any future deletion.
