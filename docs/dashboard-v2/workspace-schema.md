# Workspace Schema

`src/binance_spot_bot/dashboard_v2/workspace_schema.py` defines JSON-serializable workspace dataclasses for metadata, grid, panels, widgets and validation.

Validation blocks live mode, duplicate ids, missing safety widgets, unsafe dimensions, unknown refresh policies, script content and secret-like settings.
