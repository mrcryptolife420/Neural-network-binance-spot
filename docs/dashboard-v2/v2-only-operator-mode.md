# V2 Only Operator Mode

Run `python -m binance_spot_bot.cli dashboard-v2 --operator-mode --json`.

Operator mode uses compact V2 routes, safe action policy, no-live proof and a visible Streamlit fallback link without importing Streamlit.
