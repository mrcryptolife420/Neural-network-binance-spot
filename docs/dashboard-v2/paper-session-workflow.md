# Paper Session Workflow

Run `python -m binance_spot_bot.cli dashboard-v2-paper-session-flow-smoke --json`.

The paper flow keeps one primary action visible, keeps stop accessible, shows risk blockers and exposes session report links.
