# Streamlit Only Inventory

Run `python -m binance_spot_bot.cli dashboard-v2-streamlit-only-inventory --json`.

The inventory lists Streamlit render functions and any Streamlit-only pages/actions found by best-effort static analysis.
