# Removal Readiness Gate

Run `python -m binance_spot_bot.cli dashboard-v2-removal-readiness-gate --json`.

The gate returns `remove_now`, `keep_legacy`, `blocked_cleanup_required` or `unsafe`. It never deletes code itself.
