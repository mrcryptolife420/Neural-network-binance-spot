# V2-Only Release Evidence

Run `python -m binance_spot_bot.cli dashboard-v2-only-release-evidence-export --json`.

The bundle includes removal gate, dependency isolation, archive, V2-only release simulation, docs lock, rollback drill and no-live proof.
