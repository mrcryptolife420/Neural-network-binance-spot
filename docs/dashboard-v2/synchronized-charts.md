# Synchronized Charts

`dashboard_v2.chart_sync` provides local chart sync state for time range, crosshair timestamp, pause state, replay index, symbol and overlays for signals, fills and risk blocks.
