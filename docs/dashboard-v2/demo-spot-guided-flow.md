# Demo Spot Guided Flow

Run `python -m binance_spot_bot.cli dashboard-v2-demo-spot-flow-smoke --json`.

The flow checks demo profile, connectivity, order preview, test order, guarded demo place, reconciliation and evidence. It is demo-only.
