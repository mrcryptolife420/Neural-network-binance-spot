# Navigation Consolidation

Run `python -m binance_spot_bot.cli dashboard-v2-navigation-map --json`.

All page registry items are mapped into operator groups. Advanced pages are collapsed by default.
