# Legacy Archive

Run `python -m binance_spot_bot.cli dashboard-v2-legacy-archive-create --json`.

The archive stores Streamlit UI files, inventory, hashes, rollback command and no-live proof before any future removal execution.
