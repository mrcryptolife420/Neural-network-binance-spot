# Operator Preferences

Preferences store default workspace, mode, source, symbol, interval, chart limits, theme, compact mode and local metrics retention.

Live mode and live trading flags are blocked.
