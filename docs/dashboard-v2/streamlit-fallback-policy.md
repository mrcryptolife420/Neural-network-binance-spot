# Streamlit Fallback Policy

Streamlit remains a legacy fallback in Roadmap 107.

- Use `python -m binance_spot_bot.cli dashboard --legacy-streamlit`.
- Use Dashboard V2 first when cutover readiness is A/B.
- Removal requires a separate roadmap and gate.
