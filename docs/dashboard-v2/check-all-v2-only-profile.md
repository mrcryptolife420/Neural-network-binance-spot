# Check-All V2-Only Profile

Run `python -m binance_spot_bot.cli dashboard-v2-check-all --profile v2-only --json`.

The profile verifies V2 imports, API smoke, no-live proof, critical workflow lock and support/evidence without Streamlit.
