# Final Parity Lock

Run `python -m binance_spot_bot.cli dashboard-v2-final-parity-lock --json`.

The report maps every page registry item to a Dashboard V2 route and blocks critical gaps before V2-first cutover.
