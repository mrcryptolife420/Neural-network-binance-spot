# Workspace Grid

The React workspace page renders saved panels in a responsive CSS grid. Locked safety panels stay visible while non-safety widgets can be reviewed, cloned, exported or reset through local commands.
