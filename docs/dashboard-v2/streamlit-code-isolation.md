# Streamlit Code Isolation

Run `python -m binance_spot_bot.cli dashboard-v2-streamlit-isolation-plan --json`.

The current plan is `plan_only`: keep compatibility, keep fallback, and ensure V2 paths do not import legacy Streamlit.
