# Command Palette

Run `python -m binance_spot_bot.cli dashboard-v2-command-palette-smoke --json`.

The command palette searches safe local actions, routes, CLI commands and playbook links. Live actions are forbidden.
