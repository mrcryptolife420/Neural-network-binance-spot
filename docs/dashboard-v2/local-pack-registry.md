# Local Pack Registry

`dashboard_v2.extension_pack_registry` stores local packs under `data/dashboard-v2/extension-packs`.

Install/uninstall operations validate packs and require confirmation for writes.
