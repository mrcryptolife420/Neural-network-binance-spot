# Evidence Bundle

Run `python -m binance_spot_bot.cli dashboard-v2-evidence-export --json`.

The bundle writes a manifest, summary and redacted artifact files under `data/dashboard-v2/evidence/<run_id>/`.
