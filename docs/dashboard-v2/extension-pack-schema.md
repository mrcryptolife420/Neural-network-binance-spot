# Extension Pack Schema

`dashboard_v2.extension_pack_schema` defines pluginless pack manifests, content, dependencies, compatibility and validation.

Validation blocks live mode, unknown pack types, unknown widgets, script fields, remote URLs, code execution fields, raw secrets and unsafe workspace templates.
