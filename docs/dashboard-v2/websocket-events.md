# Dashboard V2 WebSocket Events

Live trading: disabled.

Events include `system.health`, `runtime.snapshot`, `runtime.status`, `runtime.candle`, `runtime.signal`, `runtime.fill`, `runtime.equity`, `runtime.alert`, and `dashboard.error`.

Payloads are redacted and list fields are trimmed.
