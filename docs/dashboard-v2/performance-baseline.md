# Performance Baseline

Use `python -m binance_spot_bot.cli dashboard-v2-performance-baseline --json` to write `data/dashboard-v2/performance/baseline.json` and `.md`.

The baseline records backend/API/WebSocket/frontend/chart metrics when available, treats browser/static values as optional, and always carries the no-live proof.
