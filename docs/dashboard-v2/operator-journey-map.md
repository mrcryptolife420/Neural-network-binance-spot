# Operator Journey Map

Run `python -m binance_spot_bot.cli dashboard-v2-journey-map --json`.

The map defines beginner, paper operator and demo spot operator journeys with primary V2 routes, Streamlit fallback routes and no-live proof steps.
