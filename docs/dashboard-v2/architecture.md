# Dashboard V2 Architecture

Live trading: disabled.

Dashboard V2 adds a local Python backend, typed API payloads, a WebSocket event bus, a backend-managed runtime bridge, and a React/Vite frontend scaffold.

Streamlit remains available as a fallback while Dashboard V2 reaches parity.
