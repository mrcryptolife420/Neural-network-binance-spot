# V2-Only Release Simulation

Run `python -m binance_spot_bot.cli dashboard-v2-release-simulation --json`.

The simulation verifies Dashboard V2 static assets, V2-only smoke, release manifest and no-live proof.
