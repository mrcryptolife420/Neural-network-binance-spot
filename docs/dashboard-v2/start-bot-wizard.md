# Start Bot Wizard

Run `python -m binance_spot_bot.cli dashboard-v2-start-wizard-smoke --json`.

The wizard supports `demo`, `paper` and `testnet-readiness`, then checks source, symbol, model/scenario, risk preset, safety precheck and no-live confirmation.
