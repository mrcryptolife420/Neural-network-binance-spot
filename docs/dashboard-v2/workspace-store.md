# Workspace Store

Workspaces are stored under `data/dashboard-v2/workspaces/` with layouts, exports, reports and evidence folders.

The store validates paths, writes manifest hashes, supports clone/import/export and redacts exported payloads.
