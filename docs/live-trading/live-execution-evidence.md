# Live Execution Evidence

The evidence bundle stores safety reports, dry-run status, first-order gate status, audit hash chain verification, redaction proof, and no-unattended-live proof.

Use:

```powershell
python -m binance_spot_bot.cli live-evidence-export --json
```
