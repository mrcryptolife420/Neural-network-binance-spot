# Controlled Live Executor

The executor wraps the Roadmap 118 first-order gate, enforces session budgets, requires preview and session context, blocks orders after disarm, and requires reconciliation before the next order.
