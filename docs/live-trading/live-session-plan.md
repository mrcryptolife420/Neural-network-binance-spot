# Live Session Plan

The live session plan defines symbol scope, allowed order types, budgets, risk requirements, evidence refs, and no-unattended-live statements.

Use:

```powershell
python -m binance_spot_bot.cli live-session-plan-validate --json
```
