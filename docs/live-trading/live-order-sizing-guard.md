# Live Order Sizing Guard

The sizing guard enforces first-order caps, balance percentage caps, spread caps, and preview hash requirements.

Use:

```powershell
python -m binance_spot_bot.cli live-sizing-guard --json
```
