# Live Dry-Run Session

Live dry-run verifies evidence, account status, market-data assumptions, order preview, sizing, and expected endpoint plans without placing orders.

Use:

```powershell
python -m binance_spot_bot.cli live-dry-run --json
```
