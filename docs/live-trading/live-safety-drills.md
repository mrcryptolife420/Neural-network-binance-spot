# Live Safety Drills

Safety drills verify kill-switch behavior, disarming, order-path blocking, cancel-path readiness, open-order query readiness, and audit evidence.

Use:

```powershell
python -m binance_spot_bot.cli live-kill-switch-drill --json
python -m binance_spot_bot.cli live-cancel-drill --json
```
