# Emergency Stop Playbook

Emergency stop disarms live workflow state, blocks order paths, and records audit evidence.

Use:

```powershell
python -m binance_spot_bot.cli live-emergency-stop --json
```
