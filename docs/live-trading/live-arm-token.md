# Live Arm Token

Live arm tokens are short-lived, one-time-use approvals produced only after evidence, account verification, dry-run, preview, sizing, and drills pass.

Use:

```powershell
python -m binance_spot_bot.cli live-arm-token-create --confirm I_UNDERSTAND_LIVE_SPOT_TRADING_RISK --json
```
