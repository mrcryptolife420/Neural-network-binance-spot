# Live Evidence Prerequisite Gate

This gate verifies Roadmap 117 evidence before live dry-run work starts.

It checks dataset quality, model validation, paper replay, testnet promotion, testnet rehearsal, live candidate review, evidence hashes, secret leakage, and live contamination.

Use:

```powershell
python -m binance_spot_bot.cli live-evidence-prerequisites --json
```
