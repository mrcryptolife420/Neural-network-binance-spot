# Sandbox Restore Drill

`restore_drill` extracts a backup into a temporary sandbox, checks restore preview, verifies backup, checks state integrity, checks permission drift, writes a drill report, and cleans the sandbox unless asked to keep it.

