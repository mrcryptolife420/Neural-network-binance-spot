# Codex Task Pack Generator

Run:

```powershell
python -m binance_spot_bot.cli codex-task-packs --roadmap 090 --json
```

Outputs are written under `data/roadmaps/task-packs/<number>/`.

Live trading enabled: false.
