# Controlled Restore Executor

`restore_execute` defaults to preview behavior.

Actual restore requires successful preview, successful backup verification, and exact confirmation. It creates a pre-restore snapshot and writes a restore journal.

