# One-Click Launcher

`Start-Neural-Binance-Bot.cmd` starts the local app supervisor and opens Dashboard V2 on `127.0.0.1`.

The launcher starts safe local services only and never starts live trading.

