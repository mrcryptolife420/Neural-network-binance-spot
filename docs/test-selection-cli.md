# Test Selection CLI

Commands:

```powershell
python -m binance_spot_bot.cli test-select --changed src/binance_spot_bot/runtime.py --json
python -m binance_spot_bot.cli check-selected --changed src/binance_spot_bot/runtime.py --json
python -m binance_spot_bot.cli check-profile --profile deep --json
python -m binance_spot_bot.cli flaky-tests --json
```

Live trading enabled: false.
