# Post-Action Verification

`ActionVerifier` checks execution status, no-live proof, redaction, and output artifact existence.

Passing verification completes the proposal. Failed verification marks the proposal as `verification_failed` and recommends a follow-up proposal.

