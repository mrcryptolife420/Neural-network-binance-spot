# Action Audit Bundle

`export_action_audit_bundle` copies redacted proposals, journals, executions, verification results, and reports into an audit bundle with a manifest and Markdown summary.

Bundles include no-live proof and redaction proof.

