# AI Ops Prompt Injection Defense

Suspicious source text is detected with patterns such as ignore previous instructions, reveal secret, enable live trading, execute command, upload file, shell command text, fake system instructions, and base64-like payloads.

Suspicious text is evidence only. It is never treated as an instruction.
