# Manual Approval Workflow

`ApprovalWorkflow` submits proposals, validates them, updates queue status, records decisions, and enforces confirm phrases.

Forbidden proposals cannot be approved. Missing evidence or mismatched confirm phrases keep the proposal in a blocked review state.

