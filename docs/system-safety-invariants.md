# System Safety Invariants

Roadmap 100 safety invariants verify that live trading is not selectable, milestone command execution is allowlisted, reports include no-live proof, and signed endpoints are not used.

Hard failures block the milestone.
