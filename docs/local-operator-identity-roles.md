# Local Operator Identity And Roles

Local roles are `viewer`, `operator`, `maintainer`, and `admin_local`.

Roles are local-only and do not use cloud auth. No role can enable live trading, signed orders, account endpoints, or secrets reveal.

