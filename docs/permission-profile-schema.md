# Permission Profile Schema

Permission profiles define role scopes and explicit denials. Forbidden scopes are globally blocked and cannot be granted by any profile.

`permission_matrix()` exports profiles with a manifest hash and `live_trading_enabled=false`.

