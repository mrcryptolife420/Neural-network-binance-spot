# AI Ops Local LLM Optional

Rules-only mode is the default and requires no LLM. Local LLM mode may be added as an optional redacted-context adapter. Remote LLM mode is disabled by default.

LLM output must pass the same safety validation as rules-only output.
