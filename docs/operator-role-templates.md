# Operator Role Templates

Default roles are viewer, operator, maintainer, governance reviewer, and admin local.

Admin local can manage local permission artifacts, but cannot override forbidden live/order/account/secrets scopes.

