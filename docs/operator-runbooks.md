# Operator Runbooks

Operator runbooks are machine-readable and exportable as JSON and Markdown. Each runbook has id, title, trigger, severity, steps, expected outputs, escalation, done criteria, and safety notes.

Runbook commands are validated by the local job allowlist.
