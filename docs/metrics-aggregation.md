# Metrics Aggregation

Daily and weekly aggregation summarize raw metric rows into deterministic counts, averages, minimums, maximums, anomalies, and row counts.

Aggregates are reproducible from raw JSONL metrics.
