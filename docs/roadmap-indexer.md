# Roadmap Indexer

`roadmap-index` scans `Roadmap docs/`, `Voltooid docs/`, `docs/`, and runbooks.

It reports numbers, status, checkboxes, linked modules, linked tests, linked docs, duplicate numbers, missing numbers, and the next available roadmap number.

Live trading enabled: false.
