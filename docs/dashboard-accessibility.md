# Dashboard Accessibility

Dashboard UI rules:

- Use clear visible labels for controls.
- Keep destructive actions behind confirmation checkboxes.
- Preserve Streamlit's native keyboard navigation and focus handling.
- Use tables for evidence that operators need to inspect or compare.
- Keep status language explicit: demo, paper, testnet-readiness, and live-disabled must be visible.
