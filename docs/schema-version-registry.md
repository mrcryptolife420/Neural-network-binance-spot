# Schema Version Registry

The schema registry tracks config, data store, public cache, metrics, action center, permissions, compliance, backup, deployment, portfolio, AI ops, and release manifest domains.

