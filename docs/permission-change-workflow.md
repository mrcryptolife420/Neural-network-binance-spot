# Permission Change Workflow

Permission changes are proposed, diffed, validated against forbidden scopes, approved by `admin_local` with `PERMISSION_CHANGE`, written locally, and paired with a rollback file.

