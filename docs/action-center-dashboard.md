# Action Center Dashboard

The dashboard has an `Action Center` tab with explicit proposal creation, approval queue summary, proposal detail, command preview, approve/reject/defer controls, and decision journal.

It shows `HUMAN-IN-THE-LOOP REQUIRED - NO LIVE TRADING` and does not execute unapproved actions.

