# State Integrity Checks

`state_integrity_check` is read-only by default.

It detects invalid JSON, invalid JSONL, forbidden files, and generates repair plan items such as manual review or quarantine with confirmation.

