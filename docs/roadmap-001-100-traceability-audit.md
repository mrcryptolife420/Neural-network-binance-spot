# Roadmap 001-100 Traceability Audit

The traceability audit indexes `Roadmap docs/` and `Voltooid docs/`, detects missing or duplicate roadmap numbers, and links available tests and docs.

Run:

```powershell
python -m binance_spot_bot.cli roadmap-traceability-audit --range 001-100 --json
```
