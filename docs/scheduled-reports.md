# Scheduled Reports

Default scheduled report jobs include daily paper deployment, daily data quality, daily local ops snapshot, weekly governance report, weekly support bundle verification, and weekly data growth budget.

Reports are written under `data/local-ops/reports/` and include `live_trading_enabled: false`.
