# Data Directory Migration Preview

`data_dir_migration_preview` compares source and target directories, detects conflicts, requires a backup first, and returns a rollback plan.

The flow is preview-first and does not enable live trading.

