# Operator Sign-Off Workflow

Operator sign-off is local and paper-only. It cannot approve live trading and blocks approval text that includes live approval wording.

Paper approval requires `PAPER_OS_SIGNOFF` or `APPROVE_PAPER_OS_ONLY`.
