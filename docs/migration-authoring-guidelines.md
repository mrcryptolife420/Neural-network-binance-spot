# Migration Authoring Guidelines

Every migration needs an id, dry-run, validation, explicit affected paths, rollback plan, tests, no network calls, no live/signed/order/account calls, and redacted outputs.

