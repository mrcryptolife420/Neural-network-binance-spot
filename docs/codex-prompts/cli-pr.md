# CLI PR Prompt

Add or update CLI commands for the selected roadmap.

Keep commands offline, JSON-capable, redacted, and no-live.

Run parser smoke, focused command tests, and `check-all --skip-tests`.

Live trading enabled: false.
