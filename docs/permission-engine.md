# Permission Engine

The permission engine evaluates operator roles against scopes and explains denials with missing or forbidden scope details.

It denies by default and treats disabled operators as blocked.

