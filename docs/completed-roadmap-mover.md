# Completed Roadmap Mover

`roadmap-move-completed` defaults to dry-run unless the exact confirm phrase is supplied.

It refuses to overwrite completed files and writes a move manifest after a confirmed move.

Live trading enabled: false.
