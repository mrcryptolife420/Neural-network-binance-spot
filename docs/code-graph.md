# Code Graph

`code-graph` builds a Python AST import and symbol map for `src/binance_spot_bot`.

It reports modules, imports, symbols, large modules, high fan-in, and high fan-out.

Live trading enabled: false.
