# Local Ops Metrics

Local ops metrics summarize job definitions, runs, failures, stale jobs, report freshness, scheduler health, and support bundle creation signals.

The output is dashboard-ready and includes `live_trading_enabled: false`.
