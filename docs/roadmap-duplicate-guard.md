# Roadmap Duplicate Guard

`roadmap-duplicate-guard` blocks reused roadmap numbers, filename/title mismatches, and roadmaps that already exist in completed docs.

Use it before creating a new roadmap and before moving one to `Voltooid docs/`.

Live trading enabled: false.
