# Model Strategy Validation

Model strategy validation checks dataset quality, split governance, baseline comparison, confidence calibration, risk compatibility, drawdown limits, overfit gaps, latency assumptions, and market regime coverage.

The result is research evidence only. It must not claim guaranteed profit.

Use:

```powershell
python -m binance_spot_bot.cli model-validation-run --json
```
