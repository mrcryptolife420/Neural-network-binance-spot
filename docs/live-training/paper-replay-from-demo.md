# Paper Replay From Demo

Paper replay runs demo-derived features through paper-only validation and produces equity, signal agreement, drawdown, fee/slippage, and blocker evidence.

It cannot place orders and does not require API keys.

Use:

```powershell
python -m binance_spot_bot.cli paper-replay-from-demo --json
```
