# Feature/Label Dataset

The feature/label builder creates demo-derived feature rows, labels, leakage reports, coverage reports, and hash manifests.

The builder must run without API keys and must not place orders.

Use:

```powershell
python -m binance_spot_bot.cli demo-feature-label-build --json
```
