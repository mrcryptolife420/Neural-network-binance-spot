# No-Live Proof Pack

The no-live proof pack records environment, runtime mode, command allowlist, signed endpoint, support bundle redaction, and paper-only model/portfolio scope checks.

Run:

```powershell
python -m binance_spot_bot.cli no-live-proof-pack --json
```
