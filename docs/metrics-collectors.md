# Metrics Collectors

Collectors convert existing local artifacts into metrics without mutating the trading runtime. Covered sources include check-all, dashboard smoke, support bundles, artifact catalog, data growth, evidence, and local job runs.

Missing artifacts become warning metrics instead of exceptions.
