# Test Inventory Engine

`test-inventory` scans `tests/`, detects pytest functions, unittest-style classes, imports, likely domains, safety relevance, recommended commands, and manifests.

Live trading enabled: false.
