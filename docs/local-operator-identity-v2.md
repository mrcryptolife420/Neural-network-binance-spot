# Local Operator Identity V2

`LocalOperatorIdentityStore` stores local operator profiles with hashed machine id, role ids, disabled state, profile hash, and an append-only identity journal.

Unknown operators fall back to viewer-level access.

