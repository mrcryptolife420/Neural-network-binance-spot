# Offline Backup Package

`create_offline_backup` writes a local zip with redacted files, no-live proof, redaction proof, restore instructions, inventory manifest, and backup summary.

It stores relative paths only and performs no remote upload.

