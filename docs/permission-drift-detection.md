# Permission Drift Detection

Permission drift detects manifest changes, new entries, changed values, missing manifests, and forbidden scopes.

Critical drift returns `blocked` and should block sensitive approvals.

