# Restore Preview V2

`restore_preview` inspects a backup without writing to the target directory.

It reports files to create, overwrite, skip, conflicts, forbidden paths, verification status, and no-live proof.

