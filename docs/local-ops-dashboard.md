# Local Ops Dashboard

The dashboard exposes local paper operations through the existing Ops Automation panel. It shows local job status, scheduled jobs, runbooks, reminders, calendar evidence, and local paper-only status.

Raw JSON belongs in debug expanders; primary operator views should stay readable and action-oriented.
