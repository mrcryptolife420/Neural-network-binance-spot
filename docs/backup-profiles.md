# Backup Profiles

Default profiles are `minimal_ops`, `paper_ops_full`, `audit_evidence`, and `restore_drill_fixture`.

Exclude rules always win over include rules. Unsafe local credential patterns are excluded from every profile.

