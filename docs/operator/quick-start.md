# Quick Start

Live trading: disabled.

Run `python -m binance_spot_bot.cli validate-config`, `python -m binance_spot_bot.cli preflight`, `python -m binance_spot_bot.cli check-all --skip-tests --json`, and `python -m binance_spot_bot.cli dashboard-smoke --seconds 1`.
