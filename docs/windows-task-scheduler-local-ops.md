# Windows Task Scheduler Local Ops

Windows scheduler helpers generate PowerShell scripts for local scheduler tick, daily paper reports, and weekly governance reports.

Scripts set:

- `LIVE_TRADING_ENABLED=false`
- `KILL_SWITCH=true`

Install and uninstall helpers require confirmation strings and keep paths with spaces quoted.
