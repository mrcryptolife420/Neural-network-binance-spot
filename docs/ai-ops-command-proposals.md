# AI Ops Command Proposals

Command proposals are proposal-only. They include command, args, safety class, reason, expected output, confirmation requirement, forbidden conditions, related runbook, and `no_auto_execute: true`.

Proposals are validated against the local job allowlist.
