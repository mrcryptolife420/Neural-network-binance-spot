# Paper Ops Calendar

The paper ops calendar converts local jobs into JSON, Markdown, and local `.ics` files.

Calendar items cover scheduled jobs, report due dates, governance reviews, support bundle review, evidence expiry, and browser smoke schedule.
